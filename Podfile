# Uncomment this line to define a global platform for your project

source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'

target 'Pagination_Example' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 3.4'
  pod 'SwiftyJSON'
  pod 'SDWebImage', '~>3.8'
  # Pods for Pagination_Example

  target 'Pagination_ExampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Pagination_ExampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
